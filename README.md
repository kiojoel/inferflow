# Interflow – Model Serving Platform

Interflow is a lightweight, scalable platform for serving machine learning models in production.  
It combines a Go‑based API gateway (for routing, rate‑limiting, and observability) with Python FastAPI workers that handle inference requests.  